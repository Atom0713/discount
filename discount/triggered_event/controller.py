from discount.common.utility import get_app_context


def reserved_codes_check():
    get_app_context()

    # todo 1. get all merchants that use discounts, 2. get all reserved but not used discount codes for specific merchant,
    #  3. verify its validity period (mark as un-reserved if expired), 4. repeat steps 2 and 3

    return ''
